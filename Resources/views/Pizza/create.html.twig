{% extends "AcmePizzaBundle::layout.html.twig" %}

{% block title parent() ~ " - Create" %}

{% block content %}

<h1>Create a Pizza</h1>

{{ form.setTemplate("AcmePizzaBundle::table_layout.html.twig") }}

{#
<form method="post" action="#" {{ form.enctype }}>
    {{ form.errors }}

    {% for field in form %}
        {{ field.label('My label') }}
        {{ field.errors }}
        {{ field.widget }}
    {% endfor %}

    {{ form.rest }}
    <input type="submit" />
</form>
#}

<form method="post" action="{{ path("pizza_pizza_create") }}" {{ form.enctype }}>
    {{ form.widget }}

    {{ form.rest }}
    <input type="submit" />
</form>

{% endblock %}
